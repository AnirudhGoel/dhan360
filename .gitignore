# Python
backend/.venv/
services/*/.venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
services/*/.env

# Local data — NEVER commit a user's financial data.
# Anchor to the runtime SQLite dirs only (a bare `data/` also matched
# frontend/src/engine/data — the bundled reference data — and dropped it from the build).
/data/
backend/data/
*.db
*.db-wal
*.db-shm
backend/.env

# Node / frontend
frontend/node_modules/
frontend/dist/
npm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/

# TS build cache
frontend/*.tsbuildinfo

# Personal launch notes (not published)
docs/launch.md
docs/PLAN_xirr_and_license.md
